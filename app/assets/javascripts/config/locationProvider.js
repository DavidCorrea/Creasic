creasic.config(function($locationProvider) {
    /*
    TODO: Find what to change on Rails side so we can use non # urls.
    $locationProvider.html5Mode({
        enabled: true,
        requireBase: false
    });
    */
});